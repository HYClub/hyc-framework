# 90 · 编辑器工具链

> 编辑器代码量约为运行时的 1.8 倍——这个框架的价值大半在工具链。

## 全部菜单入口

### 配置（Config）
| 菜单 | 实现 | 用途 |
| --- | --- | --- |
| `HYC Framework/Config/Config Window` | `ConfigWindow` | 配置窗口（生成/导出入口） |
| `HYC Framework/Config/Data Editor` | `ConfigDataWindow` | 数据编辑器（填表） |
| `HYC Framework/Tools/Validate Config` | `ConfigValidator` | 配置校验 |

### 本地化（Localization）
| 菜单 | 实现 | 用途 |
| --- | --- | --- |
| `HYC Framework/Localization/Import Excel` | `LocalizedExcelReader` | 导入多语言 Excel（依赖 NPOI） |
| `HYC Framework/Localization/Key Browser` | `LocaleWindow` | 浏览/搜索本地化 Key |
| `HYC Framework/Localization/Sensitive Words` | `SensitiveWordWindow` | 敏感词库管理 |
| `HYC Framework/Localization/Settings` | `LocalizationSettings` | 本地化设置 |

### UI
| 菜单 | 实现 | 用途 |
| --- | --- | --- |
| `HYC Framework/UI/Generate UI Binders` | `ComponentBinderCodeGenerator` | 生成强类型组件绑定代码 |

### 行为树
| 菜单 | 实现 | 用途 |
| --- | --- | --- |
| `Tools/HYC/BT Editor` | `BTGraphWindow` | 行为树编辑器画布 |

### 通用工具
| 菜单 | 实现 | 用途 |
| --- | --- | --- |
| `HYC Framework/Settings/Project Settings` | `QKEditorSettingsWindow` | 项目设置 |
| `HYC Framework/Tools/Show Dependents of Selection` | `AssetTools` | 查看谁引用了选中资源 |
| `HYC Framework/Tools/Clean Missing Asset References` | `AssetTools` | 清理丢失引用 |

---

## 目录结构

```
Editor/
├── BT/                    行为树编辑器（10 个 .cs）
│   ├── BTGraphWindow      EditorWindow 宿主（IMGUIContainer）
│   ├── BTGraphIMGUI       画布主体（NodeCanvas 风格 IMGUI 手绘）
│   ├── BTEditorStyles     样式与配色
│   ├── BTNodeCatalog      节点目录（分类 / 显示名 / 参数）
│   ├── BTBlobBuilder      BTTreeAsset → BTRootBlob
│   ├── BTValidator        校验
│   ├── BTCustomNodeScanner 扫描自定义节点子类
│   ├── BTNodeCreatorWindow 自定义节点代码生成
│   └── BTAutoRegisterOnPlay 进 Play 自动构建注册
├── DataEditor/            配置管线编辑器主体（44 个 .cs）
├── Dlls/                  NPOI 等 Excel 读取依赖（仅编辑器）
└── *.cs                   根散落 17 个（ConfigWindow / LocaleWindow / 组件绑定 / 敏感词 / 设置 / AssetTools）
```

---

## 依赖

| 依赖 | 用途 | 范围 |
| --- | --- | --- |
| NPOI（`Dlls/`） | 读取 Excel（多语言导入、配置源表） | 仅编辑器，不进运行时包体 |

NPOI 相关文件：`NPOI.dll`、`NPOI.OOXML.dll`、`NPOI.OpenXml4Net.dll`、`NPOI.OpenXmlFormats.dll`、`ICSharpCode.SharpZipLib.dll`、`BouncyCastle.Crypto.dll`。

---

## 各工具要点

### 数据编辑器（Data Editor）

`DataEditor/` 是整个工具链最大的一块（44 个文件），涵盖：
配置数据树、字段抽屉（Bool/Enum/List/Line/File/Folder/Multiline/ReadOnly/Position/Size/Max/TextureSize…）、ID 分配（`LocalConfigIdProvider` / `NetworkConfigIdProvider`）、导出服务、模板编辑器、枚举编辑器、预览窗口、图标选择器、错误窗口。

### 组件绑定代码生成

`ComponentBinderCodeGenerator` 依据 `ComponentBinderTable` 生成 `class X : IComponentBinder`，解决**跨场景后引用失效**——视图（重）显示时 `Reset` 重新绑定。

### 资源工具（AssetTools）

- **Show Dependents of Selection**：反查引用，改公共配置/资源前先看影响面
- **Clean Missing Asset References**：清理丢失引用（**会改资产，执行前先提交/备份**）

---

## 注意事项 / 坑

1. **`Clean Missing Asset References` 会直接改资产文件**——跑之前确保工作区干净，方便回滚。
2. **NPOI 只在编辑器可用**——别在运行时代码里引用 `LocalizedExcelReader` 或 NPOI 类型。
3. **`DataEditor/` 大量字段抽屉依赖 `ConfigFieldType`**——加新字段类型时，抽屉渲染要同步加分支，否则新类型在编辑器里显示不出来。
4. **BT 编辑器是纯 IMGUI**，没有 UXML/USS；改界面直接改 `BTGraphIMGUI.cs` 的绘制代码。
5. **画布不做 GUIClip 裁剪**——新增任何面板/浮层都要画在画布之后，否则会被节点盖住或反过来盖住节点。
